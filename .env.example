# Copy to `.env` at the repository root. That one file is read by docker compose, by
# `make start`, by `make backend` and by `make cli`: Compose will only read an environment file
# from the repository root, so every other entry point reads it from the same place.
POSTGRES_PASSWORD=market_lens
POSTGRES_PORT=5432
APP_PORT=8080

# Optional. Leave empty and the application provisions its own signing key on first start and
# keeps it in the database, so a backup is a complete installation and a restore keeps every
# session working. Set it only if you want to supply your own; once set, it must keep being
# supplied, because removing it is reported rather than silently re-keying the installation.
AUTH_SECRET=
# Session and CSRF cookies use the __Host- prefix, which browsers accept only when the cookie
# is marked Secure. Leave this true everywhere: http://localhost counts as a secure origin, so
# local development works, while setting it to false makes sign-in silently impossible.
AUTH_SECURE_COOKIES=true
# Required before provider credentials can be stored. Generate with: openssl rand -base64 32.
# Encrypts the EODHD and SMTP secrets entered in the setup wizard. Unlike AUTH_SECRET this is
# never stored in the database, because it protects secrets held inside that same database -
# keep it with your backups, or those credentials become permanently unreadable. Rotate only
# through `market-lens auth credential-key rotate`.
EXTERNAL_CREDENTIAL_KEY=
EXTERNAL_CREDENTIAL_KEY_VERSION=1
# How many recent trading sessions the nightly pass re-asks the source about, so a close the
# provider restates a day or two later is corrected rather than missed. Trading sessions on the
# instrument's own exchange, not calendar days. Costs no extra provider requests at any width —
# a range is asked for once per instrument — and a re-observation that finds nothing changed
# writes nothing. Bounded 1..60 and refused outside that, never clamped.
MARKET_DATA_REOBSERVE_SESSIONS=5

ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173
# Where a browser reaches this instance. The Vite dev server in local development, which
# proxies /api to the Go process.
APP_BASE_URL=http://localhost:5173

# The owner password, the EODHD token, and the SMTP credential are never environment values.
# They are entered once in the setup wizard and stored encrypted, and nothing reads them back.
